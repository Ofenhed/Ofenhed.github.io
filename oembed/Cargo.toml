[package]
name = "oembed"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
oembed-macro = { workspace = true, optional = true }
oembed-type.workspace = true

[dev-dependencies]
serde_json.workspace = true

[features]
macro = ["dep:oembed-macro"]
test = ["oembed-type/test"]

